var userChoice;
var correctAnswer = "A";

function compare (cChoice, correctAnswer) {
  if (uChoice === correctAnswer)
  return (whoWon = "Correct!");
  else (uChoice !== correctAnswer)
  return "Wrong!";
}

prompt("This is the question. Type A or B.")

function(){
  if u
}

alert("This is the question. Type A or B.");

do
 userChoice = prompt("A-Correct, B-Incorrect");
 if (userChoice === "A" || userChoice === "B") {
 	computerGuess();
 	alert(compare(correctAnswer, userChoice) + "\n" + "Computer Choice: " + computerChoice + "\t" + "Your Choice: " + userChoice);

 	if (confirm("Press OK to play AGAIN!") == false)
 	validChoice = true;
 }
} while(!validChoice);
